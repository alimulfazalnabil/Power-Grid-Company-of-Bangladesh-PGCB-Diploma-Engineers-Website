# PGCB Diploma Engineers Website

Community website for diploma engineers at Power Grid Company of Bangladesh.

## Development

The Alloy development environment serves the static site with Nginx on port 3000.

```sh
docker compose -f docker-compose.alloy.yaml up -d
```

Open `http://localhost:3000` directly, or `http://localhost:8080` through the Alloy preview.
