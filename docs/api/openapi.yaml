openapi: 3.1.0
info:
  title: PGCB Portal API
  version: 0.1.0
  description: API reference for the PGCB Portal backend.
paths:
  /health:
    get:
      summary: Health check
      responses:
        '200':
          description: Service is healthy
  /api/v1/health:
    get:
      summary: API health check
      responses:
        '200':
          description: API is healthy
